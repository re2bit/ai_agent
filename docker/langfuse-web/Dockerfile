ARG BASE_IMAGE=langfuse/langfuse
ARG LANGFUSE_WEB_VERSION=3.116.1
FROM ${BASE_IMAGE}:${LANGFUSE_WEB_VERSION}

# This Dockerfile extends the base langfuse-web image
# Add any customizations here if needed