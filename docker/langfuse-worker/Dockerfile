ARG BASE_IMAGE=langfuse/langfuse-worker
ARG LANGFUSE_WORKER_VERSION=3
FROM ${BASE_IMAGE}:${LANGFUSE_WORKER_VERSION}

# This Dockerfile extends the base langfuse-worker image
# Add any customizations here if needed