ARG BASE_IMAGE=clickhouse/clickhouse-server
ARG CLICKHOUSE_VERSION=latest
FROM ${BASE_IMAGE}:${CLICKHOUSE_VERSION}

# This Dockerfile extends the base clickhouse image
# Add any customizations here if needed